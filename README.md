# ibnIPS

Kotlin + Jetpack Compose starter on `kotlin_app`.

## Project Shape

- `app/` contains the Android application module.
- `agents.md` contains the branch rule: do not work on `main`, always work on `kotlin_app`.

## Next Step

Open the project in Android Studio, sync Gradle, and run the `app` configuration.
